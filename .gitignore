# Upstream repos cloned for reference only. Not ours to redistribute.
reference/

# Names the private Slack channel the roster came from, including its ID,
# member count and stated purpose. Publishing it would disclose the source far
# more explicitly than the directory does. Stays local.
plugin/data/supporters-provenance.md

# Internal Sol handoff doc. Records the privacy call, the data-confidence tiers
# and the sourcing, including the private channel by name. Not part of the
# public artifact.
FUTURE_COREY.md

# The data-half handoff doc. Names the private source channel and both channel
# IDs, so it must never be published. It lived at docs/DATA-PULL-METHOD.md until
# 2026-07-31, which was wrong twice: the build rmtree's docs/ on every run (it
# deleted the file once), and docs/ is what GitHub Pages serves.
DATA-PULL-METHOD.md

# Superseded build output location; the published build now goes to docs/.
dist/

.DS_Store
